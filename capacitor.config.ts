import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kwetzelkreative.filt',
  appName: 'Baby Dawn',
  webDir: 'dist',
  server: { androidScheme: 'https' }
};

export default config;
